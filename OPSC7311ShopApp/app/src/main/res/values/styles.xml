<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/sarinaColor</item>
        <item name="colorPrimaryDark">@color/sarinaColor</item>
        <item name="colorAccent">@color/sarinaColor</item>
    </style>

</resources>